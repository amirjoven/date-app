import { Dateconversioninfo } from './dateconversioninfo';

describe('Dateconversioninfo', () => {
  it('should create an instance', () => {
    expect(new Dateconversioninfo()).toBeTruthy();
  });
});
